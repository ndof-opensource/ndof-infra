# Opens PRs to bump the SHA-pinned reusable-workflow ref (and any future
# actions used directly by this repo). Each bump PR runs full CI before merge.
version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
