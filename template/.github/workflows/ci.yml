name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  ci:
    # Pinned CI logic: this workflow-ref SHA is maintained by ndof-infra's
    # propagate-pins workflow, which opens a PR here whenever the reusable
    # workflows or the image pin change (Dependabot cannot track a tagless
    # repo). The image digest lives only in .devcontainer/devcontainer.json;
    # CI reads it from there.
    uses: ndof-opensource/ndof-infra/.github/workflows/ci.yml@5a3fabb338d894f28c7205d619abb67284889666 # main 2026-09-10
